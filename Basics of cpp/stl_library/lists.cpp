#include<bits/stdc++.h>
#include<iostream>
#include<utility>
using namespace std;

//list is similar to vector gives front operations as well

int main(){

    list<int> ls;       //doubly linked list is the internal operation for the list while vector is maintained as single linked list
    
    ls.push_back(2);
    ls.emplace_back(2);

    ls.push_from(3);
    s


return 0;
}